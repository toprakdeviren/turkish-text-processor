// Constants and Configuration
export const CONFIG = {
    ELEMENTS_PER_THREAD: 8,
    WORKGROUP_SIZE: 256,
    STATS_BUFFER_SIZE: 20,
    PARAMS_BUFFER_SIZE: 8,
    MIN_BUFFER_SIZE: 32,
    LOCALE: 'tr-TR'
};
